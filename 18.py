# This Python code snippet prompts the user to enter a value for `N`. It then converts the input to an
# integer using `int()`.
N = int(input("Entre la valeur de N: "))

if N % 2 == 0:
    print("la valeur est Paire")
else:
    print("la valeur est Impaire")